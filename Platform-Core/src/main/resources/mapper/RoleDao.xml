<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jwzhu.platform.core.permission.db.RoleDao">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into Role(`code`, `name`, createTime, serviceId) values(#{code}, #{name}, #{createTime}, #{serviceId})
    </insert>
    <insert id="batchInsertRoleResource">
        insert into RoleResource(serviceId, roleCode, resourceCode, createTime) values
        <trim suffixOverrides=",">
            <foreach collection="resourceCodes" item="resourceCode">
                (#{serviceId}, #{roleCode}, #{resourceCode}, #{createTime})
            </foreach>
        </trim>
    </insert>
    <update id="updateById">
        update Role
        <set>
            <trim suffixOverrides=",">
                <if test="name != null">
                    `name` = #{name},
                </if>
                <if test="updateTime != null">
                    updateTime = #{updateTime},
                </if>
                <if test="serviceId != null">
                    serviceId = #{serviceId},
                </if>
            </trim>
        </set>
        where id = #{id}
    </update>
    <delete id="deleteRoleResource">
        delete from RoleResource where serviceId = #{serviceId} and roleCode = #{roleCode}
    </delete>
    <select id="queryByParam" resultType="com.jwzhu.platform.core.permission.model.Role">
        select id, `code`, `name`, createTime, updateTime, serviceId
        from Role
        <where>
            <trim suffixOverrides="and">
                <if test="id != null">
                    id = #{id} and
                </if>
                <if test="key != null">
                    (`code` like concat(#{key}, '%') or `name` like concat(#{key}, '%')) and
                </if>
                <if test="serviceId != null">
                    serviceId = #{serviceId} and
                </if>
            </trim>
        </where>
    </select>
    <select id="getById" resultType="com.jwzhu.platform.core.permission.model.Role">
        select id, `code`, `name`, createTime, updateTime, serviceId
        from Role
        where id = #{id}
    </select>
</mapper>